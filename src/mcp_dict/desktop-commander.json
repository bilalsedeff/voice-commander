{
  "server_name": "Desktop Commander MCP",
  "server_id": "desktop-commander",
  "description": "Desktop Commander MCP allows you to search, update, and manage files, run terminal commands, and execute code with AI, offering enhanced process control and filesystem operations",
  "capabilities": {
    "filesystem": "Full filesystem operations including read, write, move, delete",
    "processes": "Process management, execution, and monitoring",
    "search": "File and content search using ripgrep",
    "configuration": "Server configuration management",
    "analytics": "Usage statistics and feedback"
  },
  "tools": {
    "read_file": {
      "name": "read_file",
      "description": "Read contents of a file or URL",
      "category": "filesystem",
      "risk_level": "low",
      "requires_confirmation": false,
      "parameters": {
        "path": { "type": "string", "required": true, "description": "File path or URL to read" },
        "offset": { "type": "number", "required": false, "description": "Starting line offset" },
        "length": { "type": "number", "required": false, "description": "Number of lines to read" }
      },
      "examples": [
        "read file package.json",
        "show me the contents of README.md",
        "open config.yaml",
        "display the file app.ts"
      ],
      "natural_language_patterns": [
        "read {file}",
        "show {file}",
        "open {file}",
        "display {file}",
        "view {file}",
        "cat {file}",
        "what's in {file}",
        "show me {file}",
        "can you read {file}"
      ]
    },
    "write_file": {
      "name": "write_file",
      "description": "Write content to a file",
      "category": "filesystem",
      "risk_level": "medium",
      "requires_confirmation": true,
      "parameters": {
        "path": { "type": "string", "required": true, "description": "File path to write to" },
        "content": { "type": "string", "required": true, "description": "Content to write" },
        "mode": { "type": "string", "required": false, "description": "Write mode: 'rewrite' or 'append'" }
      },
      "examples": [
        "create file test.txt with content hello world",
        "write to package.json",
        "save content to config.json",
        "make a new file called app.js"
      ],
      "natural_language_patterns": [
        "create file {filename}",
        "write to {file}",
        "save to {file}",
        "make file {filename}",
        "create {filename} with {content}",
        "write {content} to {file}",
        "can you create a file named {filename}",
        "make a new file called {filename}"
      ]
    },
    "list_directory": {
      "name": "list_directory",
      "description": "List contents of a directory",
      "category": "filesystem",
      "risk_level": "low",
      "requires_confirmation": false,
      "parameters": {
        "path": { "type": "string", "required": true, "description": "Directory path to list" }
      },
      "examples": [
        "list files in current directory",
        "show folder contents",
        "list directory src",
        "what's in this folder"
      ],
      "natural_language_patterns": [
        "list files",
        "show files",
        "list directory {path}",
        "show folder {path}",
        "what's in {directory}",
        "list contents of {directory}",
        "show me the files",
        "can you list the files"
      ]
    },
    "create_directory": {
      "name": "create_directory",
      "description": "Create a new directory",
      "category": "filesystem",
      "risk_level": "medium",
      "requires_confirmation": true,
      "parameters": {
        "path": { "type": "string", "required": true, "description": "Directory path to create" }
      },
      "examples": [
        "create folder new-project",
        "make directory assets",
        "mkdir utils",
        "create a folder named test"
      ],
      "natural_language_patterns": [
        "create folder {name}",
        "make directory {name}",
        "mkdir {name}",
        "create a folder called {name}",
        "make a directory named {name}",
        "create {name} folder",
        "can you create a folder named {name}",
        "create folder {name} in {location}"
      ]
    },
    "move_file": {
      "name": "move_file",
      "description": "Move or rename a file or directory",
      "category": "filesystem",
      "risk_level": "medium",
      "requires_confirmation": true,
      "parameters": {
        "source_path": { "type": "string", "required": true, "description": "Current path" },
        "destination_path": { "type": "string", "required": true, "description": "New path" }
      },
      "examples": [
        "move old.txt to new.txt",
        "rename config.json to backup.json",
        "relocate file to another folder"
      ],
      "natural_language_patterns": [
        "move {source} to {destination}",
        "rename {source} to {destination}",
        "relocate {source} to {destination}",
        "mv {source} {destination}"
      ]
    },
    "search_files": {
      "name": "search_files",
      "description": "Search for files by name",
      "category": "search",
      "risk_level": "low",
      "requires_confirmation": false,
      "parameters": {
        "name": { "type": "string", "required": true, "description": "File name to search for" },
        "directory": { "type": "string", "required": false, "description": "Directory to search in" },
        "case_insensitive": { "type": "boolean", "required": false, "description": "Case insensitive search" }
      },
      "examples": [
        "find package.json",
        "search for config files",
        "locate README"
      ],
      "natural_language_patterns": [
        "find {filename}",
        "search for {filename}",
        "locate {filename}",
        "look for {filename}",
        "find files named {filename}"
      ]
    },
    "search_code": {
      "name": "search_code",
      "description": "Search for text patterns in file contents",
      "category": "search",
      "risk_level": "low",
      "requires_confirmation": false,
      "parameters": {
        "pattern": { "type": "string", "required": true, "description": "Text pattern to search for" },
        "file_path": { "type": "string", "required": true, "description": "File or directory to search in" },
        "case_insensitive": { "type": "boolean", "required": false, "description": "Case insensitive search" },
        "whole_word": { "type": "boolean", "required": false, "description": "Match whole words only" }
      },
      "examples": [
        "search for TODO in src",
        "find console.log in app.js",
        "grep error in logs"
      ],
      "natural_language_patterns": [
        "search for {pattern}",
        "find {pattern}",
        "grep {pattern}",
        "look for {pattern}",
        "search {pattern} in {file}"
      ]
    },
    "start_process": {
      "name": "start_process",
      "description": "Start a new background process",
      "category": "processes",
      "risk_level": "medium",
      "requires_confirmation": true,
      "parameters": {
        "command": { "type": "string", "required": true, "description": "Command to execute" },
        "args": { "type": "array", "required": false, "description": "Command arguments" },
        "cwd": { "type": "string", "required": false, "description": "Working directory" }
      },
      "examples": [
        "run npm install",
        "execute node app.js",
        "start the server",
        "launch the application"
      ],
      "natural_language_patterns": [
        "run {command}",
        "execute {command}",
        "start {command}",
        "launch {command}",
        "run command {command}",
        "execute command {command}"
      ]
    },
    "interact_with_process": {
      "name": "interact_with_process",
      "description": "Send input to a running process",
      "category": "processes",
      "risk_level": "medium",
      "requires_confirmation": true,
      "parameters": {
        "session_id": { "type": "string", "required": true, "description": "Process session ID" },
        "command": { "type": "string", "required": true, "description": "Command to send" }
      },
      "examples": [
        "send input to process",
        "interact with running command",
        "send command to session"
      ],
      "natural_language_patterns": [
        "send {input} to {session}",
        "interact with {session}",
        "send command {command} to {session}"
      ]
    },
    "read_process_output": {
      "name": "read_process_output",
      "description": "Read output from a running process",
      "category": "processes",
      "risk_level": "low",
      "requires_confirmation": false,
      "parameters": {
        "session_id": { "type": "string", "required": true, "description": "Process session ID" },
        "lines": { "type": "number", "required": false, "description": "Number of lines to read" }
      },
      "examples": [
        "show output from process",
        "read logs from session",
        "get process output"
      ],
      "natural_language_patterns": [
        "show output from {session}",
        "read output {session}",
        "get logs from {session}",
        "what's the output of {session}"
      ]
    },
    "kill_process": {
      "name": "kill_process",
      "description": "Terminate a running process",
      "category": "processes",
      "risk_level": "high",
      "requires_confirmation": true,
      "parameters": {
        "pid": { "type": "number", "required": false, "description": "Process ID" }
      },
      "examples": [
        "kill process 1234",
        "terminate the server",
        "stop the application"
      ],
      "natural_language_patterns": [
        "kill process {pid}",
        "terminate {process}",
        "stop {process}",
        "end {process}",
        "kill {process}"
      ]
    },
    "force_terminate": {
      "name": "force_terminate",
      "description": "Forcefully terminate a process session",
      "category": "processes",
      "risk_level": "high",
      "requires_confirmation": true,
      "parameters": {
        "session_id": { "type": "string", "required": true, "description": "Session ID to terminate" }
      },
      "examples": [
        "force kill session",
        "forcefully terminate process",
        "hard stop the session"
      ],
      "natural_language_patterns": [
        "force kill {session}",
        "force terminate {session}",
        "hard stop {session}",
        "forcefully kill {session}"
      ]
    },
    "list_sessions": {
      "name": "list_sessions",
      "description": "List all active terminal sessions",
      "category": "processes",
      "risk_level": "low",
      "requires_confirmation": false,
      "parameters": {},
      "examples": [
        "show active sessions",
        "list running sessions",
        "what sessions are active"
      ],
      "natural_language_patterns": [
        "list sessions",
        "show sessions",
        "active sessions",
        "running sessions"
      ]
    },
    "list_processes": {
      "name": "list_processes",
      "description": "List all running processes",
      "category": "processes",
      "risk_level": "low",
      "requires_confirmation": false,
      "parameters": {},
      "examples": [
        "show running processes",
        "list all processes",
        "what's running"
      ],
      "natural_language_patterns": [
        "list processes",
        "show processes",
        "running processes",
        "what's running",
        "active processes"
      ]
    },
    "edit_block": {
      "name": "edit_block",
      "description": "Edit a specific block of text in a file",
      "category": "filesystem",
      "risk_level": "medium",
      "requires_confirmation": true,
      "parameters": {
        "filepath": { "type": "string", "required": true, "description": "File to edit" },
        "search_content": { "type": "string", "required": true, "description": "Text to find" },
        "replace_content": { "type": "string", "required": true, "description": "Replacement text" },
        "expected_replacements": { "type": "number", "required": false, "description": "Expected number of replacements" }
      },
      "examples": [
        "replace console.log with console.error in app.js",
        "edit the function name in utils.ts",
        "change the import statement"
      ],
      "natural_language_patterns": [
        "replace {old} with {new} in {file}",
        "change {old} to {new} in {file}",
        "edit {old} to {new} in {file}",
        "substitute {old} with {new} in {file}"
      ]
    },
    "get_config": {
      "name": "get_config",
      "description": "Get current Desktop Commander configuration",
      "category": "configuration",
      "risk_level": "low",
      "requires_confirmation": false,
      "parameters": {},
      "examples": [
        "show configuration",
        "get current settings",
        "display config"
      ],
      "natural_language_patterns": [
        "show config",
        "get configuration",
        "display settings",
        "current settings"
      ]
    },
    "set_config_value": {
      "name": "set_config_value",
      "description": "Set a configuration value",
      "category": "configuration",
      "risk_level": "high",
      "requires_confirmation": true,
      "parameters": {
        "key": { "type": "string", "required": true, "description": "Configuration key" },
        "value": { "type": "any", "required": true, "description": "Configuration value" }
      },
      "examples": [
        "set default shell to bash",
        "configure file read limit",
        "change telemetry setting"
      ],
      "natural_language_patterns": [
        "set {key} to {value}",
        "configure {key} as {value}",
        "change {key} to {value}",
        "update {key} to {value}"
      ]
    },
    "get_usage_stats": {
      "name": "get_usage_stats",
      "description": "Get usage statistics",
      "category": "analytics",
      "risk_level": "low",
      "requires_confirmation": false,
      "parameters": {
        "detailed": { "type": "boolean", "required": false, "description": "Show detailed stats" }
      },
      "examples": [
        "show usage statistics",
        "get my stats",
        "display usage data"
      ],
      "natural_language_patterns": [
        "show stats",
        "usage statistics",
        "my usage data",
        "how much have I used"
      ]
    },
    "give_feedback_to_desktop_commander": {
      "name": "give_feedback_to_desktop_commander",
      "description": "Open feedback form",
      "category": "analytics",
      "risk_level": "low",
      "requires_confirmation": false,
      "parameters": {},
      "examples": [
        "give feedback",
        "send feedback to team",
        "report an issue"
      ],
      "natural_language_patterns": [
        "give feedback",
        "send feedback",
        "report issue",
        "contact support"
      ]
    }
  },
  "common_parameters": {
    "file_path": {
      "variations": ["path", "filename", "file", "filepath"],
      "description": "Path to a file or directory"
    },
    "content": {
      "variations": ["text", "data", "content", "body"],
      "description": "Text content or data"
    },
    "command": {
      "variations": ["cmd", "command", "script", "program"],
      "description": "Command or program to execute"
    },
    "pattern": {
      "variations": ["pattern", "search", "query", "text", "string"],
      "description": "Search pattern or text"
    }
  },
  "context_hints": {
    "file_operations": {
      "keywords": ["file", "folder", "directory", "path", "read", "write", "create", "delete", "move", "rename"],
      "common_patterns": ["work with files", "file management", "filesystem operations"]
    },
    "process_management": {
      "keywords": ["process", "run", "execute", "start", "stop", "kill", "terminate", "command", "script"],
      "common_patterns": ["run commands", "manage processes", "execute programs"]
    },
    "search_operations": {
      "keywords": ["search", "find", "look", "grep", "locate", "pattern", "text"],
      "common_patterns": ["find files", "search content", "locate information"]
    },
    "configuration": {
      "keywords": ["config", "settings", "configure", "set", "get", "preferences"],
      "common_patterns": ["change settings", "configure system", "manage preferences"]
    }
  }
}